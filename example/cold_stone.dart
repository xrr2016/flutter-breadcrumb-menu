import 'package:flutter/material.dart';

class ColdStone extends StatelessWidget {
  static final String routeName = '/cold-stone';

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(),
      body: Center(child: Text('冷石科技')),
    );
  }
}
